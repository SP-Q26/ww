# Whispering Woods Events · whisperingwoodsevents.com

**Wedding venue** marketing site (WeWeb canvas + optional git export).

## Git + Vercel (current plan)

| Item | Value |
|------|--------|
| Repo branch | **`venue`** |
| Vercel project | `ww-events` (root `.`, not this folder) |
| Runbook | `docs/events/VERCEL_VENUE_PREVIEW.md` |

This directory (`sites/events/`) is reserved for a **static-only** fallback (`public/` + local `vercel.json`). The live path is **full WeWeb export at repo root** on branch `venue`.

## WeWeb

- Editor: `https://unyielding-publisher-editor.weweb.io/`
- Publish target: GitHub `SP-Q26/ww` → branch **`venue`**

## Cross-link

Luxe senior product may link to `https://whisperingwoodsevents.com` when zero-dropoff rules allow.

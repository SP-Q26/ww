---
description: WW monorepo — commits and tracked files are SP + noreply only; run verify before push
alwaysApply: true
---

# `ww` repo · git identity (hard law)

See also `docs/luxe/LEGAL_LINKS_CANON.md` for customer-facing URLs.

## Required (repo-local)

```bash
git config user.name "S.P."
git config user.email "293159210+SP-Q26@users.noreply.github.com"
git config core.hooksPath .githooks
```

## Before every commit / push

```bash
./scripts/verify-git-identity.sh
```

## After WeWeb GitHub publish (required)

WeWeb uses operator email → GH007 with block enabled. Cleanup then push:

```bash
git fetch origin
./scripts/cleanup-git-authors-after-weweb.sh
./scripts/push-main-after-cleanup.sh
```

Runbook: `docs/WEWEB_GITHUB_PUBLISH.md`. Agent: run cleanup + verify before any `git push` on `ww` after WeWeb activity.

## Customer contact in this repo

Use **@whisperingwoodsluxe.com** / **@whisperingwoodsevents.com** only — never operator personal mail or WeWeb login email.
